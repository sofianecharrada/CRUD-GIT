{% extends 'base.html.twig' %}

{% block title %}Détails du Cours - {{ cours.name }}{% endblock %}

{% block body %}
<section class="py-12 bg-gray-50">
    <div class="container mx-auto max-w-4xl bg-white p-8 rounded-xl shadow-lg">
        <!-- Titre du cours -->
        <h1 class="text-4xl font-extrabold text-gray-800 mb-6">{{ cours.name }}</h1>

        <!-- Vérification si un fichier est associé au cours -->
        {% if cours.filePath %}
            <div class="bg-green-100 border border-green-300 p-4 rounded-md mb-6">
                <p class="text-lg text-gray-800 mb-4">Ce cours contient une pièce jointe :</p>
                <!-- Lien de téléchargement du fichier -->
                <a href="{{ path('cours.download', {'id': cours.id}) }}" class="inline-block px-6 py-3 bg-blue-600 text-white font-medium text-lg rounded-lg hover:bg-blue-700 focus:ring-2 focus:ring-blue-500 focus:outline-none transition duration-300 ease-in-out">
                    📄 Télécharger la pièce jointe
                </a>
            </div>
        {% else %}
            <div class="bg-yellow-100 border border-yellow-300 p-4 rounded-md mb-6">
                <p class="text-lg text-gray-800">Aucune pièce jointe disponible pour ce cours.</p>
            </div>
        {% endif %}

        <!-- Retour à la liste des cours -->
        <div class="text-center mt-6">
            <a href="{{ path('cours.index') }}" class="text-sm text-gray-600 hover:text-gray-800 transition duration-200 ease-in-out">
                <i class="fas fa-arrow-left"></i> Retour à la liste des cours
            </a>
        </div>

        <!-- Bouton Retour en haut -->
        <div class="text-right mt-6">
            <a href="#" class="text-sm text-gray-600 hover:text-gray-800 transition duration-200 ease-in-out" onclick="window.scrollTo({top: 0, behavior: 'smooth'})">
                <i class="fas fa-arrow-up"></i> Retour en haut
            </a>
        </div>
    </div>
</section>
{% endblock %}
